import {CapacitorConfig} from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kaywild.mitarbeiterverwaltung',
  appName: 'Connect',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
